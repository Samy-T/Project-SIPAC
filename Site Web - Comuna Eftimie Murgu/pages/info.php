<?php

    // Initialize the session
    session_start();
    // Include config file
    require_once "../login/config.php";

    sql = "SELELCT accommodation_name, address, email, phone_number, no_of_persons, price FROM accommodations WHERE id = "



